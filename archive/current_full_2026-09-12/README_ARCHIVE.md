# Frozen copy of `current/`, 12 September 2026

Full pipeline as it stood before `current/` was cut down to the figures used in the blog draft of
12 September 2026 (27 figures, every table). `data/` is not copied: point `00_setup.R` at
`../../data` (repository root) or at the original deliveries under `../` if you rerun. `output/` holds the
tables and figures produced on this date, so any dropped figure can be lifted from here as is.
